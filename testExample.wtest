import seres.*
import example.pepita

describe "group of tests for starWars" {

  test "yoda pierde esperanza" {
    const amigoArturo = new ConseguirAmigo( amigo = arturito)
    yoda.viveSuceso(amigoArturo)
    assert.equals(yoda.esperanza(),550)
  }

}